import * as React from 'react';
import styled from 'styled-components'
import securedPage from '../hocs'

const {{pascalCase componentName}} = ({children, t}) => {
  return ()
}

export default securedPage({{pascalCase componentName}})
